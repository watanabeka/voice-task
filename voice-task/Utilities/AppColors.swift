import SwiftUI

extension Color {
    // MARK: - Background
    static let appBackground = Color(hex: "#F8F9FA")
    static let appWhite = Color.white
    static let separator = Color(hex: "#ECEFF1")

    // MARK: - Text
    static let textPrimary = Color(hex: "#2C3E50")
    static let textSecondary = Color(hex: "#95A5A6")
    static let textDone = Color(hex: "#BDC3C7")

    // MARK: - Accent
    static let recordingRed = Color(hex: "#E74C3C")
    static let defaultCategoryColor = Color(hex: "#4A90D9")
}
